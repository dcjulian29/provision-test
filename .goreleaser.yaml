project_name: provision-test
before:
  hooks:
    - go mod tidy
    - go mod verify
builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64

archives:
  - builds_info:
      group: root
      owner: root
      mode: 0755
    name_template: "dcjulian29.apps.provision-test_{{ .Version }}_{{ .Os }}"
    format_overrides:
      - format: zip
        goos: windows

nfpms:
  - package_name: dcjulian29.apps.provision-test
    homepage: https://github.com/dcjulian29/cli-tools/provision-test/
    maintainer: Julian Easterling <julian@julianscorner.com>
    license: Apache 2.0
    description: |-
      provision-test is a wrapper to test ansible playbooks inside a vagrant environment.
    formats:
      - deb
      - rpm
    bindir: /usr/local/bin

chocolateys:
  - name: dcjulian29.apps.provision-test
    authors: Julian Easterling <julian@julianscorner.com>
    project_url: https://github.com/dcjulian29/cli-tools/provision-test/
    copyright: 2023 Julian Easterling julian@julianscorner.com
    license_url: https://github.com/dcjulian29/cli-tools/blob/main/LICENSE
    require_license_acceptance: false
    bug_tracker_url: https://github.com/dcjulian29/cli-tools/issues
    description: |
      provision-test is a wrapper to test ansible playbooks inside a vagrant environment.
    skip_publish: true

checksum:
  name_template: 'checksums.txt'

snapshot:
  name_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^admin:'
      - '^docs:'
      - '^test:'
